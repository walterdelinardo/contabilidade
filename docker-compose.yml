version: '3.8'

services:
  # Backend Flask
  backend:
    build: ./sistema-contabil-backend
    container_name: sistema-contabil-backend
    environment:
      - FLASK_ENV=production
      - DATABASE_URL=postgresql://bwqQCWau3ybME2RR:6XHFzssSRgbjzIMUZat3i557497Zcr38@212.85.15.242:5433/n8n
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - OPENAI_API_BASE=https://api.openai.com/v1
      - SMTP_HOST=${SMTP_HOST}
      - SMTP_PORT=${SMTP_PORT}
      - SMTP_USER=${SMTP_USER}
      - SMTP_PASSWORD=${SMTP_PASSWORD}
    ports:
      - "5000:5000"
    networks:
      - sistema-contabil-network
    restart: unless-stopped

  # Frontend React
  frontend:
    build: ./sistema-contabil-frontend
    container_name: sistema-contabil-frontend
    depends_on:
      - backend
    ports:
      - "80:80"
    networks:
      - sistema-contabil-network
    restart: unless-stopped

networks:
  sistema-contabil-network:
    driver: bridge
